name: test

on: pull_request

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: minimum example
        uses: ./

      - name: with args
        uses: ./
        with:
          args: testdata/template.yaml
